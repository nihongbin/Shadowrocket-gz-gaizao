# S1.1 Logfix Candidates

本文件记录第二轮日志里出现、但本轮暂不进入默认 S1.1 配置的域名。

这些域名需要下一轮日志确认后再决定是否进入 `references/china-local-domain-seeds.txt` 或其他规则清单，避免把测试站、不确定业务域名或非 App 关键链路默认直连。

## 暂不进入默认配置

- `itdog.cn`：偏测试/网络诊断用途，不属于中国 App 核心体验链路；默认直连可能干扰泄露测试判断。
- `ip.cn`：IP/DNS 查询站点，默认直连会直接暴露真实出口，不适合作为中国 App 白名单。
- `qualcomm.cn`：虽然是 `.cn`，但当前缺少明确 App 关键链路证据；等待下一轮日志确认。
- `qianwen.com`：当前只看到不确定子域线索，暂不把全域加入中国直连；等待下一轮日志确认。
